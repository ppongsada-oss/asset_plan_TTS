# คู่มือการใช้งานระบบ Asset Plan

> ระบบวางแผนและบริหารจัดการครุภัณฑ์สำหรับโครงการก่อสร้างและคลังกลาง  
> Version: 2.0 | อัปเดต: 2026-06-02

---

## สารบัญ

1. [ภาพรวมระบบและบทบาทผู้ใช้](#1-ภาพรวมระบบและบทบาทผู้ใช้)
2. [Site Plan — การกรอกแผนความต้องการ](#2-site-plan--การกรอกแผนความต้องการ)
3. [PM Approval — การตรวจสอบและอนุมัติแผน](#3-pm-approval--การตรวจสอบและอนุมัติแผน)
4. [Store Center — จัดการใบงาน](#4-store-center--จัดการใบงาน-แท็บ-1)
5. [Store Center — ความต้องการสุทธิ](#5-store-center--ความต้องการสุทธิ-แท็บ-2)
6. [Matrix Report — รายงานภาพรวม](#6-matrix-report--รายงานภาพรวม)
7. [Master Data — ข้อมูลครุภัณฑ์](#7-master-data--ข้อมูลครุภัณฑ์)
8. [Admin — ตั้งค่าระบบ](#8-admin--ตั้งค่าระบบ)
9. [ขั้นตอนการทำงานทั้งระบบ](#9-ขั้นตอนการทำงานทั้งระบบ)
10. [คำถามที่พบบ่อย](#10-คำถามที่พบบ่อย)

---

## 1. ภาพรวมระบบและบทบาทผู้ใช้

Asset Plan ช่วยให้ทีมโครงการ (Site) แจ้งความต้องการครุภัณฑ์ล่วงหน้าเป็น "รอบการวางแผน" และให้ทีมคลังกลาง (Store Center) จัดสรรและบริหารสต็อกได้อย่างเป็นระบบ

### บทบาทผู้ใช้งาน

| Role | ชื่อเรียก | หน้าที่ | เข้าถึงหน้า |
|---|---|---|---|
| **STORE_SITE** | ผู้กรอกแผน | กรอกแผนความต้องการของไซต์งาน | `/site-plan` |
| **PROJECT_MANAGER** | ผู้อนุมัติ | ตรวจสอบและอนุมัติ/ตีกลับแผนของไซต์ | `/site-plan/pm-approval` |
| **VIEWER** | ผู้ดูอย่างเดียว | ดูข้อมูลแต่ไม่แก้ไข | `/site-plan` (อ่านอย่างเดียว) |
| **STORE_CENTER** | ทีมคลังกลาง | จัดการรอบแผนและจัดสรรครุภัณฑ์ | `/store-center` |
| **ADMIN** | ผู้ดูแลระบบ | จัดการผู้ใช้, โครงการ, บทบาท | `/admin`, ทุกหน้า |

> **หมายเหตุ:** ผู้ใช้ 1 คนมีได้หลาย Role ในหลายโครงการ (เช่น เป็น STORE_SITE ในโครงการ A และ VIEWER ในโครงการ B)

---

## 2. Site Plan — การกรอกแผนความต้องการ

### 2.1 หน้าหลัก — รายการใบงาน `/site-plan`

หน้านี้แสดงใบงาน (Planning Job) ทุกใบที่โครงการของคุณเข้าร่วม

**การกรองตามโครงการ**
- ด้านบนมี **ปุ่มกรอง** แยกตามชื่อโครงการ
- เลือก "โครงการทั้งหมด" เพื่อดูทุกโครงการที่คุณมีสิทธิ์

**การอ่านใบงาน (Job Card)**

แต่ละใบงานแสดง: รอบแผน (Cycle), จำนวนเดือนเป้าหมาย, วันปิดรับ, สถานะ

| สถานะ | สี Badge | ความหมาย |
|---|---|---|
| OPEN | น้ำเงิน | รอบแผนเปิด กรอกข้อมูลได้ |
| SUBMITTED | เหลืองอำพัน | ส่งแผนแล้ว รอ PM อนุมัติ |
| APPROVED | เขียว | PM อนุมัติแล้ว ส่งให้คลังกลาง |
| CLOSED | เทา | ปิดรอบแล้ว |

**Badge นับถอยหลัง**

| สถานะเวลา | สี | ข้อความ |
|---|---|---|
| เลยกำหนดแล้ว | แดง (กระพริบ) | "เกินกำหนด X วัน" |
| วันนี้เป็นวันสุดท้าย | เหลือง (กระพริบ) | "ปิดรับวันนี้!" |
| เหลือ 1–3 วัน | เหลือง | "เหลือ X วัน" |
| เหลือ 4+ วัน | เทา | "เหลือ X วัน" |

**สัญลักษณ์พิเศษ**
- 🔒 **ไอคอนกุญแจ**: ใบงานถูกล็อก — แก้ไขไม่ได้  
  - ล็อกเมื่อ: สถานะ APPROVED และยังไม่ได้รับการ Unlock  
  - ล็อกเมื่อ: เลยกำหนดส่งแล้วและไม่ได้รับการ Unlock
- ✅ **Badge "Unlocked"**: คลังกลางปลดล็อกชั่วคราวให้แก้ไขได้

**ปุ่มในใบงาน**
- ใบงานปกติ: **"เริ่มวางแผน"** → คลิกเพื่อกรอกข้อมูล
- ใบงานที่ถูกล็อก: **"ดูรายละเอียด"** → คลิกเพื่อดูอย่างเดียว (แก้ไขไม่ได้)

---

### 2.2 การกรอกแผนความต้องการ `/site-plan/[job_id]`

#### โครงสร้างตาราง

| คอลัมน์ | ความหมาย |
|---|---|
| รหัส | รหัสครุภัณฑ์ |
| ชื่อรายการ | ชื่อครุภัณฑ์ |
| ยอดคลังกลาง | สต็อกที่คลังกลางมีอยู่ปัจจุบัน |
| ยอดมีอยู่ | ของที่ไซต์นี้มีอยู่แล้ว (inventory ของไซต์) |
| เดือน 1, 2, 3 ... | จำนวนที่ต้องการในแต่ละเดือนเป้าหมาย |

#### วิธีกรอกข้อมูล

1. **ค้นหาครุภัณฑ์**: พิมพ์รหัสหรือชื่อในช่องค้นหาด้านบน
2. **เรียงลำดับ**: คลิก header คอลัมน์ใดก็ได้เพื่อ sort
3. **กรอกจำนวน**: พิมพ์ตัวเลขในช่องเดือนที่ต้องการ
4. **กด Enter หรือคลิกออก** → ระบบจะ **คัดลอกค่าไปยังเดือนถัดไปอัตโนมัติ** (เฉพาะช่องที่ยังว่าง)
5. แก้ไขแต่ละเดือนได้อิสระ

**ค่าช่วยเหลือในตาราง**
- ช่องเดือนแรกจะมีตัวเลขจาง ๆ = จำนวนรอบแผนก่อนหน้า (ใช้เป็น reference เท่านั้น ไม่ใช่ค่าที่บันทึก)
- หากเดือนไหนมีค่าน้อยกว่าเดือนก่อน → มี Badge สีแดงเล็ก ๆ **"คืน X"** (ระบบรู้ว่าจะมีการคืนของ)

#### การบันทึก

| ปุ่ม | สถานะที่เปลี่ยน | สิ่งที่เกิดขึ้น |
|---|---|---|
| **"บันทึกฉบับร่าง"** | ยังคง OPEN | บันทึกโดยไม่ส่ง — กลับมาแก้ไขได้ |
| **"ส่งแผน"** | OPEN → SUBMITTED | บันทึก + ส่งให้ PM ตรวจสอบ |

> ⚠️ **หลังกด "ส่งแผน"** ตารางจะถูกล็อก — แก้ไขไม่ได้จนกว่า PM จะตีกลับ หรือคลังกลางจะ Unlock

---

## 3. PM Approval — การตรวจสอบและอนุมัติแผน

### 3.1 PM Approval Hub `/site-plan/pm-approval`

| แท็บ | เนื้อหา |
|---|---|
| **"รออนุมัติ"** | ใบงานที่ไซต์ส่งมา (สถานะ SUBMITTED) |
| **"ประวัติ"** | ใบงานที่ดำเนินการแล้ว (APPROVED / REJECTED / CLOSED) |

### 3.2 การตรวจสอบรายละเอียด

คลิกใบงานเพื่อเปิดหน้า Review

**แถบสรุปด้านบน**
- **ยอดสั่งเพิ่มรวม**: รวมทุก Equipment ที่ Demand เพิ่มขึ้นในรอบนี้
- **ยอดคืนของรวม**: รวมทุก Equipment ที่จะส่งคืนในรอบนี้

**การกรองตาราง**

| Mode | แสดง |
|---|---|
| ALL | ทุกรายการ |
| DEMAND | รายการที่ต้องการเพิ่มกว่าที่มี |
| RETURN | รายการที่จะส่งคืน |
| CHANGED | รายการที่ PM แก้ไขตัวเลขแล้ว |

**การแก้ไขก่อนอนุมัติ**
- PM แก้ไขตัวเลขได้โดยตรงในตาราง (เฉพาะเมื่อสถานะ SUBMITTED)
- กด **"บันทึกการแก้ไข"** → ระบบบันทึก log ทุก cell ที่เปลี่ยนแปลง (ตรวจสอบได้ภายหลัง)

**การตัดสินใจ**

| ปุ่ม | ผลลัพธ์ |
|---|---|
| **อนุมัติ** | บันทึกการแก้ไขอัตโนมัติ → ส่งข้อมูลให้คลังกลาง (Job: APPROVED) |
| **ตีกลับ** | ต้องกรอกเหตุผล → ส่งกลับให้ไซต์แก้ไขใหม่ (Job: OPEN) |

> **หมายเหตุ:** หากสถานะไม่ใช่ SUBMITTED ปุ่มทั้งหมดจะถูกซ่อน — ดูได้อย่างเดียว

---

## 4. Store Center — จัดการใบงาน (แท็บ 1)

### 4.1 การสร้างรอบแผน (Planning Cycle)

รอบแผนคือกรอบเวลาที่ทุกไซต์กรอกข้อมูลพร้อมกัน

**ขั้นตอนสร้างรอบใหม่:**

1. กด **"+ สร้างรอบแผนใหม่"**
2. กรอก **วันเริ่มต้น** และ **วันสิ้นสุด** (Deadline สำหรับไซต์)
3. เลือก **เดือนเป้าหมาย** — คลิก Toggle แต่ละเดือน เปลี่ยนปีได้ด้านบน (เลือกได้หลายเดือน)
4. ค้นหาและเลือก **โครงการที่เข้าร่วม**:
   - มีปุ่ม **"เลือกทั้งหมด"** / **"ยกเลิกทั้งหมด"**
   - กรองโครงการตามประเภท (SITE / ทั้งหมด) หรือค้นหาชื่อ
5. กด **"สร้าง"** → ระบบสร้าง Job ให้ทุกโครงการที่เลือกอัตโนมัติ

**Validation ก่อนสร้าง**
- ไม่เลือกเดือน → แจ้ง: _"กรุณาเลือกเดือนอย่างน้อย 1 เดือน"_
- ไม่เลือกโครงการ → แจ้ง: _"กรุณาเลือกโครงการอย่างน้อย 1 โครงการ"_

> ⚠️ **ข้อจำกัดหลังสร้าง:** วันที่เริ่ม/สิ้นสุด และเดือนเป้าหมาย **แก้ไขไม่ได้อีก** — มีเพียงโครงการที่เพิ่ม/ลบได้

### 4.2 Color Coding ของ Cycle Card

| สีขอบ | ความหมาย |
|---|---|
| แดง (rose) | เลยกำหนดส่งแล้ว |
| เหลือง (amber) | ใกล้ deadline — เหลือ ≤ 3 วัน |
| น้ำเงิน (indigo) | ปกติ ยังมีเวลา |

### 4.3 การแก้ไขรอบ (หลังสร้างแล้ว)

คลิก **Edit** ที่ Cycle Card

- **เพิ่มโครงการ**: เลือกโครงการใหม่จากรายการ → บันทึก
- **ยกเลิกโครงการ**: Deselect โครงการที่ต้องการยกเลิก  
  - ⛔ โครงการที่ Job APPROVED แล้ว → ยกเลิกไม่ได้ (ปุ่ม Deselect ถูก Disable)
  - Alert ถ้าเลือกยกเลิกทั้งหมดแต่ไม่มีโครงการที่ยกเลิกได้: _"ไม่มีโครงการที่สามารถยกเลิกได้ (หรือโครงการที่เลือกถูก APPROVED แล้ว)"_

### 4.4 การลบรอบ

- ⛔ ลบไม่ได้ถ้ามี Job ใดๆ ที่สถานะ APPROVED: _"ไม่สามารถลบงวดงานนี้ได้ เนื่องจากมีบางโครงการได้รับการอนุมัติ (APPROVED) แล้ว"_

### 4.5 การ Lock / Unlock ใบงาน

ปุ่ม Lock/Unlock ปรากฏบน Job Card เมื่อ: Job เป็น APPROVED หรือเลยกำหนด

- **Unlock** → ระบบแสดง Confirm: _"คุณต้องการปลดล็อคการ์ดงานนี้ใช่หรือไม่?"_  
  → ไซต์สามารถกลับมาแก้ไขและส่งแผนใหม่ได้
- **Lock คืน** → ปิดการแก้ไขอีกครั้ง

---

## 5. Store Center — ความต้องการสุทธิ (แท็บ 2)

### 5.1 การอ่านหน้าความต้องการสุทธิ

**แถบด้านบน (Header Counters)**
- 🔴 **High Urgency**: จำนวนใบขอที่มี `qty > 2` (ความสำคัญสูง)
- ⬛ **Total Net Requests**: จำนวนใบขอทั้งหมดในมุมมองปัจจุบัน

**ตัวกรอง (Filter Bar)**

| ตัวกรอง | คำอธิบาย |
|---|---|
| รอบแผน | เลือกรอบที่ต้องการจัดการ |
| เดือน | Toggle แต่ละเดือน (เฉพาะเดือนในรอบนั้น) |
| สถานะ | ALL / READY / PENDING / COMPLETED |
| ค้นหา | ชื่อหรือรหัสครุภัณฑ์, ชื่อโครงการ (มีหน่วงเวลา 500ms) |

**ความหมายของ Status Filter**

| สถานะ | เงื่อนไข |
|---|---|
| READY | สต็อกพอ (`remaining_stock ≥ qty − fulfilled_qty`) และยังไม่ Completed |
| PENDING | ยังจัดสรรไม่ครบ (`fulfilled_qty < qty`) |
| COMPLETED | จัดสรรครบแล้ว (`fulfilled_qty ≥ qty`) |

**การเรียงลำดับ (Sort)**
- คลิก header คอลัมน์: โครงการ, รายการ, เดือน, จำนวน, สต็อก
- คลิกซ้ำ: ขึ้น → ลง → Reset
- Default: รายการที่ยัง Pending ลอยขึ้นก่อน

**การโหลดข้อมูล**: ระบบโหลด 50 รายการแรก และโหลดเพิ่มอัตโนมัติเมื่อ scroll ลง

---

### 5.2 แท็บ "ใบขอเบิก/จัดหา" (DEMAND)

แสดงรายการที่ไซต์ต้องการ**เพิ่มเติม**จากที่มีอยู่

**การอ่านใบขอ**
- **qty**: จำนวนสุทธิที่ต้องการ (ความต้องการ − ของที่ไซต์มีอยู่แล้ว)
- **fulfilled_qty**: จำนวนที่ตัดสินใจแล้ว (เบิก + หมุนเวียน + ซื้อ + ฯลฯ รวมกัน)
- **Modal default qty**: เมื่อเปิด Modal จัดสรร ระบบกรอก `qty − fulfilled_qty` ให้อัตโนมัติ

#### 5 วิธีจัดสรร

| วิธี | ปุ่ม | เงื่อนไข | ผลต่อ remaining_stock |
|---|---|---|---|
| เบิกจ่าย | **DISPATCH** | สต็อก ≥ จำนวนที่ขอ | ลด |
| หมุนเวียน | **CIRCULATE** | ต้องกรอก Notes | ไม่เปลี่ยน |
| สลับสเปก | **SUBSTITUTE** | ต้องกรอก Notes | ไม่เปลี่ยน |
| จัดซื้อ | **BUY** | ไม่มีเงื่อนไขพิเศษ | ไม่เปลี่ยน |
| เช่า | **RENT** | ไม่มีเงื่อนไขพิเศษ | ไม่เปลี่ยน |

> ⚠️ ปุ่ม **DISPATCH จะ Disabled อัตโนมัติ** ถ้าสต็อกคลังกลางไม่เพียงพอ

**ขั้นตอนการ DISPATCH (เบิกจ่าย)**
1. กด **"เบิกจ่าย"** ที่ใบขอ
2. ตรวจสอบจำนวน (ระบบกรอก qty ที่เหลือให้อัตโนมัติ) แก้ไขได้
3. กด **"ยืนยัน"** → สต็อกลดทันที

**Bulk Dispatch (เบิกจ่ายรวม)**
1. เลือกหลายรายการด้วย **Checkbox** ซ้ายมือ
2. กด **"เบิกจ่ายรวม"**
3. Confirm dialog แสดง **จำนวนรายการที่จะประมวลผลได้** (เฉพาะที่สต็อกพอ)
4. กด Confirm → ระบบประมวลผลทีละรายการ

> ⚠️ **สำคัญ:** Bulk Dispatch **ข้ามรายการที่สต็อกไม่พอโดยไม่แจ้งเตือน** — ตรวจสอบ fulfilled_qty หลังทำเสร็จ

---

### 5.3 แท็บ "รายการส่งคืน" (RETURN)

แสดงรายการที่ไซต์มีของ**เกินกว่า**ที่วางแผนไว้ → ต้องส่งคืนคลัง

| ปุ่ม | ความหมาย | ผลต่อสต็อก |
|---|---|---|
| **รับคืน (RECEIVE)** | รับของจากไซต์เข้าคลัง | เพิ่ม remaining_stock |
| **ปฏิเสธ (REJECT_RETURN)** | ไม่รับของคืน ให้ไซต์เก็บไว้ | ไม่เปลี่ยน |

---

### 5.4 การดูและแก้ไขประวัติ Decision

คลิก **"แก้ไข"** บนใบขอที่ Completed → Modal ประวัติ

- แสดงทุก Decision (เบิก/หมุน/ซื้อ/ฯลฯ) พร้อมวันที่และหมายเหตุ
- **ลบ Decision**: กดลบ → ระบบแสดง ConfirmModal → ยืนยัน → **สต็อก Reverse อัตโนมัติ**
  - ถ้า Decision นั้นเคย DISPATCH ไป → สต็อกจะ**เพิ่มคืน**
  - ถ้า Decision นั้นเคย RECEIVE ไป → สต็อกจะ**ลด**
- Error: _"ลบไม่สำเร็จ: [error]"_

---

### 5.5 Alert สต็อกไม่เพียงพอ

แถบแจ้งเตือน (สีแดง) ปรากฏด้านบนเมื่อความต้องการรวมเกินสต็อก:  
> _"รหัส ชื่อ: ต้องการทั้งหมด X ชิ้น แต่คลังมี Y ชิ้น (ต้องจัดหาเพิ่มอีก Z ชิ้น)"_

---

### 5.6 Export Excel

| ปุ่ม | ขอบเขตข้อมูล |
|---|---|
| **"ส่งออกตามที่กรองไว้"** | ใช้ Search + Status + Month filter ปัจจุบัน |
| **"ส่งออกทั้งหมด"** | ทั้งรอบแผน — ไม่สนใจ Search/Status/Month |

**Excel มี 2 Sheet:**
1. **New Demand** — รายการขอเบิก/จัดหา
2. **Expected Returns** — รายการส่งคืน

**คอลัมน์ใน Excel**: โครงการ, รหัสโครงการ, รายการอุปกรณ์, รหัสอุปกรณ์, หน่วย, เดือน, จำนวน, ดำเนินการแล้ว, คงเหลือ, สต็อกคลังกลาง, ความสำคัญ, หมายเหตุล่าสุด

**ชื่อไฟล์**: `CenterRequests_Cycle[N]_YYYY-MM-DD.xlsx`

---

## 6. Matrix Report — รายงานภาพรวม

เข้าที่ `/matrix-report` — เห็นความต้องการทุกไซต์ในรูป Pivot Table

### 6.1 โครงสร้างตาราง

- **แถว (Row)**: ครุภัณฑ์แต่ละรายการ (Item)
- **คอลัมน์กลุ่มไซต์** (สีน้ำเงิน): จำนวนที่แต่ละไซต์วางแผนไว้ (Peak qty ของรอบ)
- **คอลัมน์กลุ่มคลัง** (สีเขียว): สต็อกของคลังกลางแต่ละแห่ง
- **คอลัมน์สรุป**: ตัวเลขรวมและสถานะการดำเนินการ

> ค่า 0 แสดงเป็น "-" (ขีด) เพื่อให้อ่านง่าย

### 6.2 คอลัมน์สรุปที่สำคัญ

| คอลัมน์ | ความหมาย |
|---|---|
| **D รวม** | ยอด Demand สุทธิรวมทุกไซต์ (ความต้องการ − inventory ไซต์) |
| **เบิก** | รวมที่ DISPATCH แล้ว |
| **หมุน** | รวมที่ CIRCULATE แล้ว |
| **สลับ** | รวมที่ SUBSTITUTE แล้ว |
| **ซื้อ** | รวมที่ BUY แล้ว |
| **เช่า** | รวมที่ RENT แล้ว |
| **ค้างส่ง D** | ยอด Demand ที่ยังไม่ได้จัดสรร (Pending Demand) |
| **S รวม** | รวมที่ไซต์จะส่งคืน (Expected Returns) |
| **ยอดคืน** | รวมที่ RECEIVE แล้ว |
| **ปฏิเสธ** | รวมที่ REJECT_RETURN แล้ว |
| **ค้างรับ R** | ยอดส่งคืนที่ยังไม่ได้รับ (Pending Receipt) |

### 6.3 การใช้งาน

**ตัวกรอง**
- **รอบแผน**: เลือกรอบ → ระบบเลือก Months ให้อัตโนมัติ
- **เดือน**: Toggle แต่ละเดือนอิสระ (Multi-select)
- **"แสดง Archive"**: เปิด/ปิดโครงการที่ Archive แล้ว
- **"ต้องดำเนินการ"**: กรองเฉพาะแถวที่มี `ค้างส่ง D > 0` หรือ `S รวม > 0`

**การเรียงลำดับ**
- Default: แถวที่มี Pending Demand/Receipt ลอยขึ้นก่อน
- คลิก header คอลัมน์ใดก็ได้: ขึ้น → ลง → Reset

**Tooltip (เมื่อ Hover)**
- แสดง breakdown รายโครงการ: ชื่อโครงการ, จำนวนที่ต้องการ (Gross), คาดว่าจะคืน, ยอดสุทธิ
- แสดง Breakdown รายเดือน

### 6.4 Export

| ปุ่ม | เนื้อหา | ชื่อไฟล์ |
|---|---|---|
| **Export All** | ตาราง Matrix ทั้งหมด (2 Sheet) | `Matrix_Report_YYYY-MM-DD.xlsx` |
| **Export Procurement List** | เฉพาะ BUY + RENT แยกรายโครงการ/เดือน | `Procurement_Plan_YYYY-MM-DD.xlsx` |

> หาก Export Procurement แล้วไม่มีรายการ → แจ้ง: _"ไม่มีรายการจัดซื้อหรือจัดเช่าในขณะนี้"_

---

## 7. Master Data — ข้อมูลครุภัณฑ์

เข้าที่ `/master-data` — จัดการรายการครุภัณฑ์ทั้งหมดในระบบ

**การใช้งาน**
- ดูรายการครุภัณฑ์ทั้งหมด (Item Code, ชื่อ, หน่วย, ราคา, สต็อกปัจจุบัน)
- เพิ่ม/แก้ไข/ลบรายการครุภัณฑ์
- **"โหลด Template CSV"**: ดาวน์โหลด Template สำหรับ Import ข้อมูลจำนวนมาก
- **"อัปโหลดข้อมูล (Bulk)"**: Import ครุภัณฑ์หลายรายการพร้อมกันจาก CSV

> ⚠️ การแก้ไข `remaining_stock` ที่ Master Data จะกระทบยอดที่แสดงในทุกส่วนของระบบโดยตรง

---

## 8. Admin — ตั้งค่าระบบ

### 8.1 จัดการผู้ใช้ `/admin/users`

**การสร้างผู้ใช้ใหม่**
- ฟิลด์: Email (บังคับ), Password (บังคับสำหรับสร้างใหม่), Global Role
- Global Role: **USER** (ทั่วไป) / **STORE_CENTER** (ทีมคลัง) / **ADMIN** (ผู้ดูแล)

**Global Role Badge**

| Role | สี |
|---|---|
| ADMIN | แดง |
| STORE_CENTER | เหลือง |
| USER | เทา |

**การแก้ไข**: แก้ Email, Password ได้ (Password ปล่อยว่างถ้าไม่ต้องการเปลี่ยน)

**การลบ**: กด Delete → Browser Confirm → ลบถาวร (ไม่มี Disable/Soft-delete)

### 8.2 จัดการโครงการ `/admin/projects`

- สร้างโครงการใหม่ ประเภท **SITE** (ไซต์งาน) หรือ **WAREHOUSE** (คลังกลาง)
- Archive โครงการที่ไม่ใช้งาน (ยังเก็บข้อมูล แต่ซ่อนจากรายการปกติ)

### 8.3 จัดการสิทธิ์ `/admin/project-roles`

**การกำหนด Role**
1. เลือก **ผู้ใช้**
2. ค้นหาและเลือก **โครงการ** (เลือกได้หลายโครงการพร้อมกัน)
   - มีปุ่ม **"เลือกทั้งหมด"** / **"ล้าง"** (ใช้กับผลการค้นหาปัจจุบัน)
3. เลือก **Role** ที่ต้องการกำหนด:
   - **STORE_SITE** — ผู้กรอกแผน (แก้ไขและส่งแผนได้)
   - **PROJECT_MANAGER** — ผู้อนุมัติ (อนุมัติ/ตีกลับแผนได้)
   - **VIEWER** — ผู้ดูอย่างเดียว (ดูข้อมูลได้แต่แก้ไขไม่ได้)
4. กด **"Confirm Assignment"**

**การอ่านตาราง Assignment**
- จัดกลุ่มตาม ผู้ใช้ + Role
- ถ้ามี 4+ โครงการใน Group เดียวกัน → แสดง 3 ชื่อ + "+N more" (hover เพื่อดูทั้งหมด)
- กด Delete → ลบ**ทั้ง Group** (ทุกโครงการของ User+Role นั้น) พร้อม Confirm

---

## 9. ขั้นตอนการทำงานทั้งระบบ (End-to-End Flow)

```
┌─────────────────────────────────────────────────────────────────┐
│ STORE CENTER                                                    │
│  สร้างรอบแผน (Planning Cycle) → เลือกโครงการ → กำหนด Deadline │
└───────────────────────────┬─────────────────────────────────────┘
                            │ ระบบสร้าง Job ให้ทุกโครงการ
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ STORE_SITE (ทุกไซต์พร้อมกัน)                                   │
│  กรอกจำนวนครุภัณฑ์รายเดือน → บันทึกร่าง → ส่งแผน (SUBMITTED) │
└───────────────────────────┬─────────────────────────────────────┘
                            │ PM รับ Notification
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ PROJECT_MANAGER                                                 │
│  ตรวจสอบแผน → แก้ไขตัวเลข (ถ้าจำเป็น) →                      │
│  อนุมัติ (APPROVED) ← หรือ → ตีกลับ (OPEN) → ไซต์แก้ใหม่     │
└───────────────────────────┬─────────────────────────────────────┘
                            │ หลัง APPROVED
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ STORE CENTER — Center Dashboard (แท็บ 2)                       │
│  เห็น Net Demand ของทุกไซต์                                    │
│  ตัดสินใจ: DISPATCH / CIRCULATE / SUBSTITUTE / BUY / RENT     │
│  (ตามลำดับความเป็นไปได้)                                       │
└───────────────────────────┬─────────────────────────────────────┘
                            │ ของถึงไซต์
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│ STORE CENTER — จัดการของคืน (RETURN tab)                      │
│  ไซต์ส่งของที่เกินแผนคืน →                                    │
│  RECEIVE (รับเข้าคลัง) หรือ REJECT_RETURN (ให้ไซต์เก็บไว้)   │
└───────────────────────────┬─────────────────────────────────────┘
                            │ ทุกรายการ Completed
                            ▼
                     ปิดรอบ (CLOSED)
```

**Status Flow สรุป**

```
Job:  OPEN → SUBMITTED → APPROVED → CLOSED
              ↑ (PM ตีกลับ → กลับเป็น OPEN)
              ↑ (STORE CENTER Unlock → ไซต์แก้ใหม่ → SUBMITTED)

Plan: DRAFT → PENDING_APPROVAL → APPROVED / REJECTED / PROCURED
```

---

## 10. คำถามที่พบบ่อย

**Q: กรอกข้อมูลแล้ว แต่กด "ส่งแผน" ไม่ได้**  
A: ตรวจสอบว่า Job ยังเป็น OPEN และไม่ถูกล็อก หากถูกล็อก ติดต่อคลังกลางเพื่อ Unlock

**Q: ส่งแผนแล้วแต่ตัวเลขผิด ต้องการแก้ไข**  
A: ติดต่อ PM ขอให้ตีกลับ หรือติดต่อคลังกลางขอ Unlock เพื่อส่งใหม่

**Q: PM อนุมัติแล้วแต่ตัวเลขในระบบต่างจากที่กรอก**  
A: PM มีสิทธิ์แก้ไขตัวเลขก่อนอนุมัติ ดู log การเปลี่ยนแปลงใน PM Review Table (กรอง Mode: CHANGED)

**Q: ทำ Bulk Dispatch แต่บางรายการไม่ถูกประมวลผล**  
A: ระบบข้ามรายการที่สต็อกไม่เพียงพอโดยไม่แจ้ง ตรวจสอบคอลัมน์ "ยอดคลังกลาง" แล้ว DISPATCH ทีละรายการสำหรับรายการที่ขาด

**Q: ยอดสต็อกคลังกลางแสดงผิด**  
A: สต็อกอัปเดตทันทีเมื่อ DISPATCH หรือ RECEIVE หรือลบ Decision ตรวจสอบประวัติ Decision ที่หน้า Center Dashboard

**Q: ต้องการยกเลิกโครงการออกจากรอบแผน แต่ทำไม่ได้**  
A: โครงการที่ Job สถานะ APPROVED แล้ว ยกเลิกไม่ได้ — ต้องติดต่อ Admin เพื่อดำเนินการพิเศษ

**Q: Export Excel ออกมาว่างเปล่า**  
A: ตรวจสอบว่าเลือกรอบแผนถูกต้อง และใช้ "ส่งออกทั้งหมด" แทน "ส่งออกตามที่กรองไว้" เพื่อดูว่ามีข้อมูลจริงหรือไม่

**Q: ต้องการให้ผู้ใช้ดูข้อมูลได้แต่ไม่แก้ไข**  
A: กำหนด Role เป็น **VIEWER** ที่หน้า `/admin/project-roles` — ผู้ใช้จะเห็นข้อมูลได้แต่ปุ่มแก้ไขทั้งหมดจะซ่อน
